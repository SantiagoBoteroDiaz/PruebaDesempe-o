@using GestionDeEspacios.Enums
@model GestionDeEspacios.Models.SportSpace

@{
    ViewData["Title"] = "Actualizar Espacio Deportivo";
}

<div class="section">

    <!-- HEADER -->
    <div class="row valign-wrapper">
        <div class="col s12 m8">
            <h4 style="font-weight:700;">Actualizar Espacio Deportivo</h4>
            <p class="grey-text text-darken-1">
                Modifica la información del espacio registrado.
            </p>
        </div>

        <div class="col s12 m4 right-align">
            <a asp-action="Index"
               class="btn grey lighten-3 black-text waves-effect">
                <i class="material-icons left">arrow_back</i>
                Volver
            </a>
        </div>
    </div>

    <!-- FORM -->
    <div class="row">
        <div class="col s12 m10 l8 offset-m1 offset-l2">

            <div class="card z-depth-2" style="border-radius:22px;">
                <div class="card-content" style="padding:35px;">

                    <div class="center-align" style="margin-bottom:25px;">
                        <i class="material-icons orange-text text-darken-2"
                           style="font-size:55px;">edit</i>

                        <h5 style="font-weight:700; margin-top:10px;">
                            Editar Espacio Deportivo
                        </h5>
                    </div>

                    <form asp-action="Update" method="post">

                        <input type="hidden" asp-for="Id" />

                        <div asp-validation-summary="ModelOnly"
                             class="red-text"
                             style="margin-bottom:15px;">
                        </div>

                        <!-- NAME -->
                        <div class="input-field">
                            <i class="material-icons prefix orange-text text-darken-2">
                                place
                            </i>

                            <input asp-for="Name" id="Name" />
                            <label asp-for="Name"></label>
                            <span asp-validation-for="Name" class="red-text"></span>
                        </div>

                        <!-- TYPE -->
                        <div class="input-field">
                            <i class="material-icons prefix orange-text text-darken-2">
                                sports_soccer
                            </i>

                            <select asp-for="Type"
                                    asp-items="Html.GetEnumSelectList<SportSpacesType>()">
                            </select>

                            <label asp-for="Type">Tipo de deporte</label>
                            <span asp-validation-for="Type" class="red-text"></span>
                        </div>

                        <!-- CAPACITY -->
                        <div class="input-field">
                            <i class="material-icons prefix orange-text text-darken-2">
                                groups
                            </i>

                            <input asp-for="Capacity" id="Capacity" type="number" min="1" />
                            <label asp-for="Capacity"></label>
                            <span asp-validation-for="Capacity" class="red-text"></span>
                        </div>

                        <!-- BUTTONS -->
                        <div class="center-align" style="margin-top:30px;">

                            <a asp-action="Index"
                               class="btn grey lighten-3 black-text waves-effect"
                               style="border-radius:12px; margin-right:10px;">
                                Cancelar
                            </a>

                            <button type="submit"
                                    class="btn orange darken-2 waves-effect waves-light"
                                    style="border-radius:12px; padding:0 28px;">
                                <i class="material-icons left">save</i>
                                Actualizar
                            </button>

                        </div>

                    </form>

                </div>
            </div>

        </div>
    </div>

</div>

@section Scripts{
@{
    await Html.RenderPartialAsync("_ValidationScriptsPartial");
}

<script>
document.addEventListener('DOMContentLoaded', function () {

    M.FormSelect.init(document.querySelectorAll('select'));
    M.updateTextFields();

});
</script>
}